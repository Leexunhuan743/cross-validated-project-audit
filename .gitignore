# Python build artifacts (regenerable; must not ship inside the skill package)
__pycache__/
*.pyc
*.pyo
*.pyd

# Test runner caches
.pytest_cache/

# Local working notes (engineering assessments / session memory)
.workbuddy/
